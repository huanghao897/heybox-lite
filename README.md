# heybox Lite

面向方屏 Android 手表的小黑盒社区第三方客户端，重点适配 CD20 Max 一类小尺寸方屏设备。支持 Android 4.0（API 14）及以上系统。

当前正式版：1.70

## 功能

- 二维码登录与登录态加密保存
- 社区信息流、帖子详情、图片与评论
- 二级评论分批展开和收起
- 图片中心缩放全屏预览、双指缩放与原图查看
- 个人资料、我的收藏与浏览历史
- 首页下拉刷新、社区帖子搜索与历史记录搜索
- 搜索页保留最近搜索记录并支持一键清空
- 日间/夜间主题、十二组主/副色预设与缓存清理
- 界面、文字、边距及帖子正文字体/间距实时预览
- 独立界面预览页，模拟帖子、正文和评论层级
- 可配置开屏动画、开屏文字和显示时长
- 可关闭的启动自动更新检查
- 一键恢复默认显示设置
- 首页文章类型标识
- 关于页、版本信息和 GitHub Release 更新检查
- 针对小尺寸方屏的紧凑布局

## 1.70 更新

- 新增并修复点赞、收藏、评论、关注等交互操作，统一写入请求的登录态、鉴权 token 与兼容请求参数。
- 修复关注按钮“显示关注但实际未关注”的问题，优先使用帖子外层作者 ID，并按 `follow_status` 正确判断关注、互关状态。
- 优化已执行操作按钮的视觉状态：关注、帖子点赞、列表点赞和评论点赞在成功状态下会高亮显示，不再越点越暗。
- 改进请求头、Cookie 同步和写入 token 刷新逻辑，降低“请重新登录”“验证参数错误”的出现概率。
- 优化列表与详情页状态同步，点赞数量和本地缓存状态会在返回列表时保持一致。

## 1.60 更新

- 修复“我的收藏”无法显示的问题，收藏列表改为参考小黑盒网页端的 `tab_list` 与收藏内容接口调用方式。
- 收藏接口失败时优先保留并显示本地缓存，避免空 `folder_id` 请求导致误报“没有权限”。
- 优化图片缓存与列表复用，减少从帖子返回主页时图片闪烁或重新加载。
- 优化官方表情资源加载等待队列，降低异常情况下的卡住概率。
- 修复部分 Android 4.x 兼容问题，避免使用低版本不可用的集合排序 API。
- 优化更新检查、加载动画和网络失败时的状态保留。

## 构建

需要 Java 17 和 Android SDK。项目当前使用 Android API 36、AGP 8.13.2 和 Gradle 8.13。

```powershell
.\gradlew.bat assembleDebug
```

正式版本需要在本地配置签名信息后构建：

```powershell
.\gradlew.bat assembleRelease
```

签名文件、`release-signing.properties`、HAR、Cookie、构建产物和 R8
`mapping.txt` 均被排除在版本控制之外。

## 安全说明

- Android 6.0 及以上使用 Android Keystore + AES-GCM 保存 Cookie。
- Android 4.0–5.1 使用设备与应用签名绑定的 AES-CBC + HMAC 加密保存 Cookie，登录状态可跨重启保留；受旧系统能力限制，安全强度低于 Android 6.0 及以上。
- 网络请求头、接口路径和鉴权字段统一封装；release 开启 R8 full mode 与资源压缩。
- release 版本关闭调试、禁止明文网络流量与备份，并包含轻量签名完整性检查。

这些措施用于提高基础安全性，不能替代服务端鉴权和风控。

## 声明

本项目是非官方客户端，仅供学习、研究与个人使用，不代表小黑盒、HeyWear 或相关官方立场。本项目基于 HeyWear 进行二次开发与适配。第三方接口并非公开稳定 API，字段、签名和访问策略可能随时变化。请遵守相关法律法规、平台规则和服务条款，不要提交账号 Cookie、HAR 或其他敏感数据。
