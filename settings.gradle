pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "HeyBoxCommunity"
include(":app")
include(":cnwearoverlay-runtime")
project(":cnwearoverlay-runtime").projectDir = file("third_party/cnwearoverlay/runtime")
