<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Certificate pinning is intentionally disabled until a maintained backup pin exists. -->
    <base-config cleartextTrafficPermitted="false" />
    <!-- Temporary self-hosted update server. Remove this after binding a HTTPS domain. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">103.236.54.97</domain>
    </domain-config>
</network-security-config>
